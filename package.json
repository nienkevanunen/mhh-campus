{
  "name": "mhh-map",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "fetch-osm": "node scripts/fetch-osm.mjs",
    "convert-osm": "node scripts/convert-raw.mjs",
    "refresh-data": "npm run fetch-osm && npm run prepare-data",
    "prepare-data": "node scripts/prepare-data.mjs"
  },
  "devDependencies": {
    "typescript": "^5.5.3",
    "vite": "^5.4.1"
  },
  "dependencies": {
    "@types/leaflet": "^1.9.21",
    "leaflet": "^1.9.4",
    "maplibre-gl": "^5.24.0",
    "osmtogeojson": "^3.0.0-beta.5"
  }
}
